# Python Debug Reference

- Use `python --version` and `where python` to identify the interpreter.
- Prefer `python -m pip` so installation and execution use the same interpreter.
- Check virtual-environment activation and module naming collisions.
- Capture the complete exception type and the first project-owned stack frame.
- Rerun the exact command after the correction.
